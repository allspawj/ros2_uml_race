name: ros2-ci
on: [push, pull_request]
jobs:
  build:
    runs-on: [self-hosted, ros2]
    steps:
      - uses: actions/checkout@v4
      - name: Build & test
        run: |
          source /opt/ros/humble/setup.bash
          colcon build
          colcon test && colcon test-result --verbose
